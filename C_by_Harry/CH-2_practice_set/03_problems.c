//check if a number is divisibe by 97 or not
#include<stdio.h>

int main(){
    // int a;
    // printf("Enter a number = ");
    // scanf("%d",&a);
    // printf("%d\n" , a%97==0);
    int a = 485;
    printf("The remainder when a is divided by 97 is = %d\n ", a%97); // if it is 0, then divisble, otherwise non-divisble
    return 0;
}